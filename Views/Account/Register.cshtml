@model kindergarten.Models.RegisterViewModel

@{
    ViewData["Title"] = "Đăng ký tài khoản";
}

<style>
    body {
        background: linear-gradient(135deg, #ffe6ec 0%, #ffd6e0 100%);
        font-family: "Poppins", sans-serif;
    }

    .register-card {
        background-color: #fff;
        border-radius: 20px;
        box-shadow: 0 6px 20px rgba(255, 150, 180, 0.3);
        padding: 2.5rem;
        margin-top: 3rem;
        margin-bottom: 3rem;
    }

    h3.register-title {
        color: #e75480;
        font-weight: 700;
        text-align: center;
        margin-bottom: 1.5rem;
    }

    h5.section-title {
        color: #e75480;
        font-weight: 600;
        border-left: 4px solid #e75480;
        padding-left: 10px;
    }

    .form-label {
        color: #444;
    }

    .form-control:focus,
    .form-select:focus {
        border-color: #e75480;
        box-shadow: 0 0 0 0.2rem rgba(231, 84, 128, 0.25);
    }

    .btn-primary {
        background-color: #e75480;
        border: none;
        border-radius: 12px;
        transition: background 0.3s ease;
    }

    .btn-primary:hover {
        background-color: #d93b6e;
    }

    .alert {
        border-radius: 12px;
    }

    a {
        color: #e75480;
        text-decoration: none;
    }

    a:hover {
        text-decoration: underline;
    }

    hr {
        border-top: 2px dashed #ffd6e0;
        margin: 1.5rem 0;
    }

    .text-success {
        color: #e75480 !important;
    }
</style>

<div class="container">
    <div class="register-card">
        <h3 class="register-title">🌷 Đăng ký tài khoản phụ huynh 🌷</h3>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger text-center">@ViewBag.Error</div>
        }

        @if (ViewBag.Success != null)
        {
            <div class="alert alert-success text-center">@ViewBag.Success</div>
        }

        <div asp-validation-summary="All" class="text-danger mb-3"></div>

        <form asp-action="Register" method="post" novalidate>
            @Html.AntiForgeryToken()

            <h5 class="section-title mb-3"><i class="bi bi-person-fill"></i> Thông tin phụ huynh</h5>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="HoTen" class="form-label fw-semibold">Họ tên phụ huynh</label>
                    <input asp-for="HoTen" class="form-control" placeholder="Nguyễn Văn A" />
                    <span asp-validation-for="HoTen" class="text-danger small"></span>
                </div>

                <div class="col-md-6 mb-3">
                    <label asp-for="Email" class="form-label fw-semibold">Email</label>
                    <input asp-for="Email" class="form-control" placeholder="email@gmail.com" />
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="GioiTinh" class="form-label fw-semibold">Giới tính</label>
                    <select asp-for="GioiTinh" class="form-select">
                        <option value="">-- Chọn giới tính --</option>
                        <option>Nam</option>
                        <option>Nữ</option>
                    </select>
                    <span asp-validation-for="GioiTinh" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="TenDangNhap" class="form-label fw-semibold">Tên đăng nhập</label>
                    <input asp-for="TenDangNhap" class="form-control" placeholder="vd: phuhuynh01" />
                    <span asp-validation-for="TenDangNhap" class="text-danger small"></span>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="MatKhau" class="form-label fw-semibold">Mật khẩu</label>
                    <input asp-for="MatKhau" type="password" class="form-control" placeholder="********" />
                    <span asp-validation-for="MatKhau" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="XacNhanMatKhau" class="form-label fw-semibold">Xác nhận mật khẩu</label>
                    <input asp-for="XacNhanMatKhau" type="password" class="form-control" placeholder="********" />
                    <span asp-validation-for="XacNhanMatKhau" class="text-danger small"></span>
                </div>
            </div>

            <hr />

            <h5 class="section-title mb-3"><i class="bi bi-heart-fill"></i> Thông tin bé</h5>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="HoTenBe" class="form-label fw-semibold">Họ tên bé</label>
                    <input asp-for="HoTenBe" class="form-control" placeholder="Tên của bé" />
                    <span asp-validation-for="HoTenBe" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="GioiTinhBe" class="form-label fw-semibold">Giới tính bé</label>
                    <select asp-for="GioiTinhBe" class="form-select">
                        <option value="">-- Chọn giới tính --</option>
                        <option>Nam</option>
                        <option>Nữ</option>
                    </select>
                    <span asp-validation-for="GioiTinhBe" class="text-danger small"></span>
                </div>
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-primary w-100 py-2 fw-semibold">
                    💗 Đăng ký tài khoản
                </button>
            </div>

            <div class="text-center mt-3">
                <span>Đã có tài khoản?</span>
                <a asp-action="Login" class="fw-semibold">Đăng nhập ngay</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}